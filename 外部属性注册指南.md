# SX-Attribute 外部属性注册指南

本文面向服务器配置作者和第三方 Bukkit 插件开发者，适用于 SX-Attribute 4.x。

SX-Attribute 提供两种扩展方式：

1. 只编写 YAML，通过统一属性引擎创建配置化属性。
2. 编写 Java 插件，继承 `SubAttribute` 注册需要自定义 Bukkit 逻辑的属性。

优先使用 YAML。只有配置化触发器和动作无法表达需求时，才需要实现 Java 属性。

## 一、使用 YAML 创建属性

普通自定义属性不需要编写 Java 代码。

1. 在 `plugins/SX-Attribute/Feature/Attribute/definitions/` 新建一个 YAML 文件，例如 `my-attributes.yml`。
2. 在 `plugins/SX-Attribute/Feature/Attribute/Attributes.yml` 的 `Files` 中加入该文件。
3. 执行 `/sxa reload`。

示例：

```yaml
Attributes:
  FireDamage:
    Enable: true
    Values:
      Damage:
        Match: "火焰伤害"
        Aggregate: SUM
        Min: 0
        Max: 100000
        CombatPower: "<l:value>"
    Triggers:
      - Type: ATTACK
        When: "<l:self_Damage> > 0"
        Actions:
          - Type: DAMAGE
            Formula: "<l:self_Damage>"
    Display:
      Category: 元素
      Order: 10
      Format: "&c火焰伤害: {Damage}"
```

物品 Lore：

```text
火焰伤害: 10
```

读取动态属性：

```java
double damage = SXAttribute.getApi().getDynamicAttribute(player, "FireDamage", "Damage");
```

属性 ID 和字段名区分大小写，调用 API 时必须与 YAML 完全一致。

## 二、Java 插件注册属性

### 1. 添加编译依赖

将 SX-Attribute JAR 放入开发项目的 `libs` 目录，并仅作为编译依赖引用，不要打包进你的插件。

Gradle：

```groovy
dependencies {
    compileOnly files('libs/SX-Attribute-4.0.0-beta.1-all.jar')
    compileOnly files('libs/SX-Item-4.5.0-api.jar')
}
```

Maven：

```xml
<dependency>
    <groupId>github.saukiya</groupId>
    <artifactId>SX-Attribute</artifactId>
    <version>4.0.0-beta.1</version>
    <scope>system</scope>
    <systemPath>${project.basedir}/libs/SX-Attribute-4.0.0-beta.1-all.jar</systemPath>
</dependency>
```

### 2. 声明插件依赖

第三方插件必须在 `plugin.yml` 中使用 `depend`，确保 SX-Attribute 已执行 `onLoad()`，但尚未执行 `onEnable()`。

```yaml
name: ExampleAttribute
main: com.example.attribute.ExamplePlugin
version: 1.0.0
depend:
  - SX-Attribute
```

### 3. 在 `onLoad()` 注册

属性只能在第三方插件的 `onLoad()` 中注册。

```java
package com.example.attribute;

import org.bukkit.plugin.java.JavaPlugin;

public final class ExamplePlugin extends JavaPlugin {

    @Override
    public void onLoad() {
        new RageAttribute(this).registerAttribute();
    }
}
```

不要在 `onEnable()` 注册。SX-Attribute 启用后会冻结旧属性优先级表，迟到的注册请求将被拒绝。

### 4. 实现属性类

下面的示例注册一个单字段属性 `Rage`，识别 Lore `怒气: 10`。

```java
package com.example.attribute;

import github.saukiya.sxattribute.data.attribute.AttributeType;
import github.saukiya.sxattribute.data.attribute.SubAttribute;
import github.saukiya.sxattribute.data.eventdata.EventData;
import org.bukkit.configuration.file.YamlConfiguration;
import org.bukkit.entity.Player;
import org.bukkit.plugin.java.JavaPlugin;

import java.util.Collections;
import java.util.List;

/** 读取物品 Lore 中的怒气值，并向 SX-Attribute 提供聚合、面板和占位符数据。 */
public final class RageAttribute extends SubAttribute {

    public RageAttribute(JavaPlugin plugin) {
        super("Rage", plugin, 1, AttributeType.OTHER);
    }

    @Override
    protected YamlConfiguration defaultConfig(YamlConfiguration config) {
        config.set("Rage.DiscernName", "怒气");
        config.set("Rage.UpperLimit", 100D);
        config.set("Rage.CombatPower", 1D);
        return config;
    }

    @Override
    public void loadAttribute(double[] values, String lore) {
        if (lore.contains(getString("Rage.DiscernName"))) {
            values[0] += getNumber(lore);
        }
    }

    @Override
    public void correct(double[] values) {
        super.correct(values);
        values[0] = Math.min(values[0], config().getDouble("Rage.UpperLimit", 100D));
    }

    @Override
    public void eventMethod(double[] values, EventData eventData) {
        // 此示例只注册数值，不处理攻击、受击或更新事件。
    }

    @Override
    public Object getPlaceholder(double[] values, Player player, String placeholder) {
        return "Rage".equals(placeholder) ? values[0] : null;
    }

    @Override
    public List<String> getPlaceholders() {
        return Collections.singletonList("Rage");
    }

    @Override
    public double calculationCombatPower(double[] values) {
        return values[0] * config().getDouble("Rage.CombatPower", 1D);
    }
}
```

## 三、配置属性优先级

注册前必须在 `plugins/SX-Attribute/Config.yml` 的 `AttributePriority` 中加入属性。

```yaml
AttributePriority:
  - Rage#ExampleAttribute
```

格式：

```text
属性名#提供该属性的插件名
```

建议始终填写插件名，避免多个插件使用相同属性名时互相覆盖。

优先级列表中的位置决定属性执行顺序。攻击、防御等存在前后依赖的属性，应由服主明确安排顺序。

修改 `AttributePriority` 后必须完整重启服务器，不能只执行热重载。

## 四、生成的配置文件

第三方 Java 属性不会写入 `Feature/Attribute/definitions/`，而是继续使用独立配置文件：

```text
plugins/SX-Attribute/Attribute/<第三方插件名>/<属性名>.yml
```

以上示例生成：

```text
plugins/SX-Attribute/Attribute/ExampleAttribute/Rage.yml
```

`defaultConfig()` 只在文件不存在时生成默认值，不会覆盖服主已经修改的配置。

## 五、属性类型

构造 `SubAttribute` 时可以声明一个或多个 `AttributeType`：

| 类型 | 用途 |
| --- | --- |
| `ATTACK` | 攻击方属性，在伤害流程中执行 |
| `DEFENCE` | 防御方属性，在受击流程中执行 |
| `UPDATE` | 实体属性刷新时执行，通常用于生命、速度等持续状态 |
| `OTHER` | 独立事件或仅提供数值的属性 |

如果不传类型，默认使用 `OTHER`。

`eventMethod()` 收到的 `EventData` 可能是不同实现。处理前必须使用 `instanceof` 判断，禁止直接强制转换。

```java
if (eventData instanceof DamageData) {
    DamageData damageData = (DamageData) eventData;
    // 处理攻击或受击逻辑
}
```

## 六、实现 Bukkit 监听器

属性类可以同时实现 `Listener`。SX-Attribute 会在第三方插件启用时自动注册该属性监听器。

```java
public final class RageAttribute extends SubAttribute implements Listener {

    @EventHandler(ignoreCancelled = true)
    public void onExp(PlayerExpChangeEvent event) {
        double rage = SXAttribute.getApi().getEntityData(event.getPlayer()).getValues(getName())[0];
        // 使用 rage
    }
}
```

不要再次调用 `Bukkit.getPluginManager().registerEvents()`，否则监听器会被重复注册。

## 七、生命周期方法

第三方属性可以覆盖：

| 方法 | 调用时机 |
| --- | --- |
| `onEnable()` | SX-Attribute 初始化属性管理器时 |
| `onReLoad()` | 执行 `/sxa reload` 时 |
| `onDisable()` | SX-Attribute 关闭时 |

周期任务必须保存调度器返回的任务句柄，并在 `onDisable()` 中幂等取消。不要取消一个尚未调度的 `BukkitRunnable`。

Folia 环境中的实体操作必须回到实体所属区域线程。需要跨 Paper、Folia 和旧 Spigot 时，优先使用 SX-Attribute 提供的 `FoliaScheduler`。

## 八、读取和刷新属性

读取实体聚合属性：

```java
double rage = SXAttribute.getApi()
        .getEntityData(player)
        .getValues("Rage")[0];
```

让 SX-Attribute 重新扫描玩家装备：

```java
SXAttribute.getApi().updateData(player);
SXAttribute.getApi().attributeUpdate(player);
```

加载指定物品的属性：

```java
SXAttributeData data = SXAttribute.getApi().loadItemData(
        player,
        new PreLoadItem(EquipmentType.MAIN_HAND, item)
);
```

## 九、线程和事件约束

- 不要自行伪造 SX 事件的同步或异步状态。
- 实体、背包、世界和药水操作必须在正确的 Bukkit/Folia 线程执行。
- 异步数据库任务完成后，必须切回实体线程再修改玩家状态。
- 监听 SX 事件时，应根据 `event.isAsynchronous()` 决定后续调度方式。

## 十、注册失败排查

### 日志显示 `Disable [插件名|属性名]`

属性没有出现在 `AttributePriority` 中，或 `属性名#插件名` 与实际名称不一致。

### 日志显示 `SXAttribute is Enabled, Unable to register`

注册发生得太晚。把 `registerAttribute()` 从 `onEnable()` 移到 `onLoad()`。

### 属性配置没有生成

确认属性注册成功，并检查：

```text
plugins/SX-Attribute/Attribute/<插件名>/
```

### Lore 有文本但数值为零

检查 `DiscernName`、颜色代码、`loadAttribute()` 判断和 `values` 下标是否一致。

### 属性覆盖了其它属性

两个属性占用了相同优先级。为第三方属性使用 `属性名#插件名`，并确保列表中不存在重复位置或错误覆盖关系。
