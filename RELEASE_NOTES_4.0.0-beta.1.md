# SX-Attribute 4.0.0 Beta 1

这是统一配置化重构的首个公开测试版本，建议先在测试服验证配置迁移与装备数据，再用于正式服务器。

## 主要变化

- 属性定义迁移到 `Feature/Attribute/`，支持分片、自定义字段、公式条件、触发器与动作 DSL。
- 新增来源生命周期系统，支持叠层规则、持久化、三种 SQL 后端与 Redis 多服协调。
- 新增套装、品质、词缀、强化、升星、重铸、洗练、孔位和附魔成长模块，各模块均可独立开关。
- 新增 `/sxa forge` 配置化锻造界面，以及动态属性、来源和装备操作 API。
- 保留旧属性数组与既有战斗行为的兼容影子，普通自定义属性可通过 `/sxa reload` 热重载。

## 测试提示

- 升级前请备份插件目录、玩家物品数据和来源数据库。
- 根目录旧 `Attributes.yml` 已迁移为 `Feature/Attribute/Attributes.yml`；请按新目录结构迁移自定义配置。
- 自定义附魔 ID 的新增或删除仍需重启服务器，普通属性与规则可热重载。
- 首个 Beta 尚未内置跨版本真实附魔注册适配器；未由服务端预注册的自定义附魔会被明确禁用，不会降级为 Lore 伪附魔。
- SQLite 仅适用于单服；多服持久化写入要求 SQL 与 Redis 同时可用。
- 这是 Beta 版本，配置结构和 API 在正式版前仍可能调整。
