# SX-Attribute 4.0.0 Beta 2

这是统一配置化重构的第二个公开测试版本，集中修复 Beta 1 在 Arclight、高版本 Java 和新版 SX-Item 环境中的兼容问题。

## 主要修复

- JavaScript 属性现在从 SX-Item 类加载器发现 Nashorn，每个脚本独立加载与熔断，脚本异常不再影响插件本体。
- 修复主线程触发异步属性事件、异步线程触发同步事件的问题，并保持 Folia 实体操作在线程所属区域执行。
- NBT 读取对 AIR、空物品、模组物品和版本适配失败安全降级，避免攻击事件持续刷出 NBT 异常。
- 修复 SX-Item 4.5.4 移除 `ReMaterial` 枚举字段后，属性、出售和修理界面无法打开的问题。
- 条件公式新增 `>`、`<`、`>=`、`<=`、`==`、`!=`、`&&` 与 `||`，元素、亡灵克制和低生命条件可正常判断。
- 自定义附魔优先使用真实注册；服务端不支持时可通过 `Fallback: LORE` 自动切换为 NBT + 动态 Lore 成长模式。
- 装备模块改为逐模块隔离注册与重载，单个功能不兼容时不再阻断其它装备功能和插件启动。
- 增加外部属性注册指南，并补充装备 NBT 状态的兼容性测试。

## 升级提示

- 替换 JAR 后需要完整重启服务器，不要使用第三方热卸载插件。
- 已生成的旧 `JSAttribute.js` 会在启动时精确迁移到隔离的 JavaScript 引擎，不会改写其它自定义 JS 文件。
- 自定义附魔的 `Fallback` 可设为 `LORE` 或 `DISABLE`；未配置时默认使用 `LORE`。
- `DamageEvent.DamageCalculationToEVE` 只控制怪物互殴；针对具体生物请在属性公式中使用 `defender_type_<实体类型>`。
- 升级前仍建议备份插件目录、玩家物品数据和来源数据库。
